#  AWS S3 deletion scheduler

This solution is to schedule a Lambda function which accepts a list of file extensions and delete S3 objects that were uploaded / modified over a specific number of days.
